# 改名字，对应nacos里的配置文件名
spring:
  application:
    name: gulimall-coupon
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: properties # 指定配置文件为yaml/properties格式
#        namespace: 只能写命名空间的id