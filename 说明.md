启动nacos(E:\javatools\project\nacos\bin)
startup.cmd -m standalone
启动虚拟机(E:\Vagrant\centos7)
vargant up
vargant ssh

启动内网穿透(E:\javatools\neiwangchuantou)

 [run_natapp.bat](..\..\neiwangchuantou\run_natapp.bat) 地址可能会变,需要求改



启动前端(renren-fast-vue)

npm run dev





\# 基础启动（默认端口8080，账号/密码：sentinel/sentinel） java -jar sentinel-dashboard-1.8.3.jar # 自定义端口（避免和项目端口冲突，比如改8888） java -Dserver.port=8333 -jar sentinel-dashboard-1.8.3.jar # 自定义账号密码（可选） java -Dserver.port=8333 -Dsentinel.dashboard.auth.username=admin -Dsentinel.dashboard.auth.password=admin123 -jar sentinel-dashboard-1.8.3.jar



浏览器打开：`http://localhost:8333（或你自定义的端口，如 8888），输入默认账号密码 `sentinel/sentinel` 即可登录。