server:
  port: 30000

spring:
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
    alicloud:
      access-key: ${OSS-ACCESS-KEY}
      secret-key: ${OSS-SECRET-KEY}
      oss:
        endpoint: oss-cn-beijing.aliyuncs.com
        bucket: gulimall-acmdy

  application:
    name: gulimall-third-party
